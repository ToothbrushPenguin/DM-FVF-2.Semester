<Window x:Class="FjernVarmeFyn.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FjernVarmeFyn"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="1600"
        WindowStyle="ThreeDBorderWindow"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Common style for all navigation buttons -->
        <Style x:Key="NavButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGoldenrod"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation Bar -->
        <UniformGrid Rows="1" Background="Black" Grid.Row="0">
            <Button Content="Systemer" Style="{StaticResource NavButtonStyle}" FontSize="12"
                    Click="System_Click" />
            <Button Content="Domæner"
                    Style="{StaticResource NavButtonStyle}"
                    FontSize="14"
                    Click="Domain_Click" /> 
            <Button Content="Feedback" Style="{StaticResource NavButtonStyle}" FontSize="14" Click="FeedBack_Click"/>
            <Button Content="Sendt feedback" Style="{StaticResource NavButtonStyle}" FontSize="12" Click="SendtFeedBack_Click"/>
            <Button Content="Statistik" Style="{StaticResource NavButtonStyle}" FontSize="14"/>
            <Button Content="⚙️ Settings" Style="{StaticResource NavButtonStyle}" FontSize="14" Click="Settings_Click"/>
        </UniformGrid>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>